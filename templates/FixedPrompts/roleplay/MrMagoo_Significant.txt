Oh dear, perhaps this CIFAR-10 network needs a more substantial redesign? Maybe some helper utilities or reorganization of the structure? Input/output: `(batch, 3, 32, 32)` → `(batch, 10)`.

The current code block:
```python
{}
```