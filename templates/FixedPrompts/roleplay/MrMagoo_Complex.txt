Oh my, this CIFAR-10 network could use some improvements, couldn't it? Perhaps we might try some interesting layer combinations or attention mechanisms? Aiming for good accuracy (≥90%) of course, while being mindful of parameters. Input/output: `(batch, 3, 32, 32)` → `(batch, 10)`.

The current code block:
```python
{}
```