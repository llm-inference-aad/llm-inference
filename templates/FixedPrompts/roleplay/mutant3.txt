Modify the architecture of this CIFAR-10 network in an unconventional way! Consider using dynamic routing or reversible blocks. The input is of shape `(batch, 3, 32, 32)` and the expected output is of shape `(batch, 10)`. Here is the current code block:
```python
{}
```