Well now, how might we make this CIFAR-10 architecture a bit leaner? Perhaps some clever simplifications that don't hurt performance too much? Input/output: `(batch, 3, 32, 32)` → `(batch, 10)`.

The current code block:
```python
{}
```