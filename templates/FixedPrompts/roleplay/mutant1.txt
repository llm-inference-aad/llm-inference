python
import numpy as np

def generate_text(prompt, max_length):
    # Generate some random text based on the prompt
    return np.random.choice(['apple', 'banana', 'cherry'], size=max_length)

# Test the function
print(generate_text('test', 10))

```python
{}
```