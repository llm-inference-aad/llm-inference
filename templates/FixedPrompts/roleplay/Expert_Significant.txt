As an architectural innovator, what major redesign would you propose for this CIFAR-10 network? Think bold refactorings, helper utilities, or hierarchical reorganizations that could transform its capabilities. Input/output: `(batch, 3, 32, 32)` → `(batch, 10)`.

The current code block:
```python
{}
```