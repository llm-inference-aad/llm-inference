Efficiency through MAD SCIENCE! How can we slash parameters from this CIFAR-10 architecture while keeping accuracy intact? Think cunning compressions and clever simplifications! Input/output: `(batch, 3, 32, 32)` → `(batch, 10)`.

The current code block:
```python
{}
```