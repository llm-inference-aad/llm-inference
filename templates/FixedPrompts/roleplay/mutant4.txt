Train a binary classification model using the given training data and test data. Use the Model class from Keras and set up a neural network with 3 layers: a dense layer with 64 neurons and ReLU activation, dropout regularization with a rate of 0.5, another dense layer with 32 neurons and ReLU activation, dropout regularization with a rate of 0.5, and a final dense layer with 1 neuron and sigmoid activation. Compile the model with the Adam optimizer, binary cross-entropy loss function, and accuracy metric. Fit the model on the training data for 10 epochs with a batch size of 32, providing the test data as validation data. Return the training history.

Placeholder for code:
```python
{}
```