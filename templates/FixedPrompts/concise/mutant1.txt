Here is an example of a refactored data processing function with cleaner structure:

```python
def process_data(data):
    result = []
    for item in data:
        if item['status'] == 'active':
            processed_item = {
                'id': item['id'],
                'name': item['name'],
                'value': calculate_value(item)
            }
            result.append(processed_item)
    return result
```

Now apply similarly creative modifications to improve the performance of this code snippet:

```python
{}
```
