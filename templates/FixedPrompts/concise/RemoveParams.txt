Q: How could this CIFAR-10 architecture become more parameter-efficient while maintaining strong accuracy? Explore compression techniques, efficient layer designs, or clever architectural simplifications. The goal is a leaner model that doesn't compromise on performance. Input/output tensors: `(batch, 3, 32, 32)` → `(batch, 10)`.

The current code block:
```python
{}
```