Renowned for out-of-the-box helper abstractions, you now need to streamline this CIFAR-10 model under the dual objective (accuracy ≥90%, minimal parameters).

Context:
- Emphasize modular helper functions that encapsulate reusable architectural levers.
- Improve both code clarity and the accuracy/parameter trade-off.

Constraints:
- Inputs/outputs remain `(batch, 3, 32, 32)` → `(batch, 10)`.
- Helpers must be fully implemented, cohesive, and testable.

Architectural Levers:
- Helper factories for depthwise separable blocks, SE attention, or activation stacks.
- Shared pooling/aggregation helpers that reduce duplicate parameters.
- Configuration-driven channel allocation or normalization adapters.

Desired Change:
- Describe how you will build and integrate helper utilities that unlock new Pareto-efficient behavior (e.g., modular stage builder, adaptive attention helper). Highlight efficiency gains per helper.

Trade-off Analysis Required:
- Quantify parameter savings or additions per helper and justify the effect on accuracy.
- Explain validation guardrails (unit tests, assertions, monitoring hooks).
- Specify fallback paths if a helper harms convergence.

The current code block:
```python
{}
```
1. Retain original class names.
2. Format the code in Markdown as a single ```python``` fenced block.
3. Exclude setup/demonstration code.
4. Retain original tensor input/output sizes.
5. Provide full code.
6. Must function with default settings.
7. Avoid all commentary outside the fenced code block.
8. If no change is required, return the original code inside the fenced block.
9. This network targets high CIFAR-10 accuracy (≥90%) with efficient parameter usage. Consider how architectural choices influence both objectives.
10. Explore creative modifications that push performance boundaries—novel layer compositions, unconventional activation patterns, or emergent architectural motifs are encouraged.

A: Let us think step by step
