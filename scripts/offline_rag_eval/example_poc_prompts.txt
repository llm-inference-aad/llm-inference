# One mutation prompt per line (max 10 by default). Lines starting with # are skipped.
# Use your real templates or shortened copies for a quick RAG POC.

You are mutating a CNN block. Improve the following PyTorch module for accuracy: class Stem(nn.Module): ...

Suggest a single architectural change to this residual block and output valid Python only: class ResBlock(nn.Module): ...

Refactor this depthwise separable conv for efficiency on CIFAR-10: class DW(nn.Module): ...
